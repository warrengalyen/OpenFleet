import { Store } from 'lucide-react'
import { ComingSoon } from '@/components/layout/ComingSoon'

export function VendorsPage() {
  return (
    <ComingSoon
      title="Vendors"
      subtitle="Manage suppliers, repair vendors, and parts providers"
      icon={Store}
    />
  )
}
